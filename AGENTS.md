# Project Notes

Keep token and URL values out of logs. Downstream repositories pass `WATCHTOWER_URLS` and `WATCHTOWER_TOKENS` secrets to the required action inputs.

Run `make ci` before finishing changes.
